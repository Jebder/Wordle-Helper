import wordArray from '/home/jeb/repos/Wordle-Solver/words.js';

const wordList = wordArray; //maybe redundant but more readable.

function colorDeclaration(){
    alert('Click the letter to change it/s color.');
    
};